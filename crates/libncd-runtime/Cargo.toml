[package]
name = "libncd-runtime"
version = "0.1.0"
edition = "2024"

[dependencies]
libncd = { path = "../libncd" }
tokio = { workspace = true, features = [
    "net",
    "io-util",
    "sync",
    "macros",
    "rt-multi-thread",
] }
