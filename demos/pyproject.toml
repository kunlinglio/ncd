[project]
name = "ncd-demos"
version = "0.1.0"
description = "Linux user-space handlers for ncdd-created NCD character devices"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
display = ["opencv-python", "numpy"]
